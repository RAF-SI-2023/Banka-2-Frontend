import { BankAccountMaskDirective } from './bank-account-mask.directive';

describe('BankAccountMaskDirective', () => {
	it('should create an instance', () => {
		const directive = new BankAccountMaskDirective();
		expect(directive).toBeTruthy();
	});
});
